package com.jtrent238.mcforgeav;

import java.util.UUID;

public class ModUUIDs {
	// Creating a random UUID (Universally unique identifier).
    UUID uuid = UUID.randomUUID();
    String randomUUIDString = uuid.toString();
}
